#include "main.h"
/**
 * using loop to write all alphabet lowercase
 * Return: the parameter
 */
void print_alphabet(void)
{
	int n;

	for (n = 'a' ;  n <= 'z' ; n++)
		_putchar(n);
	_putchar('\n');
}
